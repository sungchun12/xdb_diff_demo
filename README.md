# xdb_diff_demo

Quick demo of xdb_diff

```bash
# setup venv
pip install --upgrade pip
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
source venv/bin/activate

# run demo for xdb diff between two duckdb databases
python xdb_diff_demo.py
```